# OpenAI SDK Example

Start MockMind from the repository root:

```bash
npm run dev -- start --config examples/openai-sdk/mockmind.yaml
```

Then run this example with your preferred TypeScript runtime:

```bash
tsx examples/openai-sdk/index.ts
```

Expected output:

```txt
Hello from the MockMind OpenAI SDK example.
```
